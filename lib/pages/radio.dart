import 'package:flutter/material.dart';

class RadioZ extends StatelessWidget {
  const RadioZ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Radio Z')));
}
